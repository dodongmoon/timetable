# timetable

3학년 2반 학생별 시간표 정적 사이트 저장소.

## 배포

GitHub Pages를 `main` 브랜치 루트에서 켜면 `index.html`이 바로 서비스된다.

## 파일

- `index.html`: 배포용 메인 페이지
- `students.json`: 학생별 선택 과목 데이터
- `generate_timetables.py`: 원본 엑셀에서 페이지를 다시 생성하는 스크립트
